import { CalculateYearsPipe } from './calculate-years.pipe';

describe('CalculateYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
